{
  "name": "motion-studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "render:example": "pnpm --filter @motion-studio/example-remotion render"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
