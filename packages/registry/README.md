# `@motion-studio/registry`

The registry stores installed plugin definitions and resolves capability
providers using deterministic rules. It owns plugin identity, capability
registration, and core compatibility checks; semantic intent filtering belongs
to the resolver phase.
