# `@motion-studio/plugin-sdk`

The plugin SDK defines the public contract for native Motion Studio plugins.
It describes plugin identity, core compatibility, capabilities, and provider
components without coupling the Motion Core to a specific plugin.
