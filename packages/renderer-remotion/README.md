# `@motion-studio/renderer-remotion`

The Remotion adapter renders a compiler-produced `RenderPlan`. It owns
Remotion composition registration, reusable visual components, and frame-level
motion primitives. It does not compile Storyboards or resolve plugins.
